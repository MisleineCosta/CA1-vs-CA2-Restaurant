FROM alpine:3.5

RUN apk add --no-cache --virtual git && apk add --no-cache --virtual curl
RUN apk update && apk add bash && apk add go
RUN git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf && bash ~/.fzf/install
RUN rm -rf /tmp \
RUN rm -rf ~/.fzf/src \
RUN rm -rf ~/.fzf/test
